#include<stdio.h>
void main()
{
    int i;  
    char arr[50];

    printf("Enter the string \n");
    gets(arr);
    printf("The strings is\n");
    puts(arr);

    for(i=0;arr[i]!='\0';++i)
    {
    
    }
    printf("The length of the String is %d",i);
 

      return;
}